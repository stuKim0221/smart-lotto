<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 기본 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 알림 관련 권한 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 카메라 기능(선택) -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lotto"
        android:enableOnBackInvokedCallback="true"
        android:resizeableActivity="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 런처 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:resizeableActivity="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 수동 입력 화면 -->
        <activity
            android:name=".ui.manual.ManualInputActivity"
            android:exported="false"
            android:theme="@style/Theme.Lotto"
            android:resizeableActivity="true" />

        <!-- QR 스캔 화면 -->
        <activity
            android:name=".ui.qr.QrScanActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
            android:resizeableActivity="true" />

        <!-- QR 결과 화면 -->
        <activity
            android:name=".ui.qr.QrResultActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:resizeableActivity="true" />

        <!-- 데이터 상태, 설정 등 -->
        <activity
            android:name=".ui.settings.DataStatusActivity"
            android:label="데이터 상태"
            android:parentActivityName=".MainActivity"
            android:resizeableActivity="true" />

        <activity
            android:name=".ui.settings.PrivacyPolicyActivity"
            android:exported="false"
            android:resizeableActivity="true" />

        <!-- AI 번호 생성 -->
        <activity
            android:name=".ui.ai.AiNumberGenerationActivity"
            android:exported="false"
            android:label="AI 번호 생성"
            android:parentActivityName=".MainActivity"
            android:resizeableActivity="true" />


        <!-- 알림용 브로드캐스트 리시버 -->
        <receiver
            android:name=".notify.DrawReminderReceiver"
            android:exported="false" />

        <!-- 부팅 후 알림 재예약용 리시버 -->
        <receiver
            android:name=".notify.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}"/>
    </application>
</manifest>